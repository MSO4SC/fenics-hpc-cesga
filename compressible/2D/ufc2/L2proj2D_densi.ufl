# Niyazi Cem Degirmenci

cell = triangle
order = 1
W = VectorElement("Lagrange",cell,order, 4)
Q = FiniteElement("Lagrange",cell,order)

v = TestFunction(Q)
u = TrialFunction(Q)
f = Coefficient(W)

a = u*v*dx
L = f[2]*v*dx
